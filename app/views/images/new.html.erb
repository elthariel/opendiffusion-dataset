<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New image</h1>

  <%= render "form", collection: @collection, image: @image %>

  <%= link_to "Back to images", collection_images_path(@collection),
              class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
